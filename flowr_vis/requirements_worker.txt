# FLOWR Visualization — GPU Worker
# ==================================
# This is the GPU worker that loads the FLOWR model
# and runs ligand generation.
#
# Requires PyTorch with CUDA support and the FLOWR package.
# ==================================

# Core web server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
requests>=2.31.0

# Chemistry
rdkit>=2024.3.1
numpy>=1.24.0

# NOTE: torch, lightning, and the flowr package itself come from the
# conda environment (flowr_root).  Do NOT pip-install them separately.
